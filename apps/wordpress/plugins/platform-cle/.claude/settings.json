{
  "permissions": {
    "allow": [
      "Bash(rm -rf /tmp/pcle-package)",
      "Bash(mkdir -p /tmp/pcle-package/platform-cle /tmp/pcle-package/platform-cle-theme)",
      "Bash(rsync -a --exclude='.DS_Store' --exclude='tests' plugin/ /tmp/pcle-package/platform-cle/)",
      "Bash(rsync -a --exclude='.DS_Store' theme/ /tmp/pcle-package/platform-cle-theme/)",
      "Bash(zip -r -X platform-cle.zip platform-cle -x '*.DS_Store')"
    ]
  }
}
